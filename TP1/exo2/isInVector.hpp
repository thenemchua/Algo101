#ifndef _IS_IN_VECTOR_HPP_
#define _IS_IN_VECTOR_HPP_

#include<vector>

bool isInVector(int number, std::vector<int> const& tab);
std::vector<int> intersect(std::vector<int> const& tab1, std::vector<int> const& tab2);

#endif
